# BurnNotice
Help visually design and deploy networks for ALU SR-OS and other network elements.

The concept is to use a web framework to create the virtual network elements, and additional configure physical elements.
